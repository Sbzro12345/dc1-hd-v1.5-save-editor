DataArtifact::createIAPItem - for recreating IAP Accessories byte for byte
DataProfile::unlockIAP - for recreating IAP Headgear, Weapon and Chestplate byte for byte
DataArtifact::createSpecialItem - for recreating Special Items byte for byte


pay attention to the set(), addSpecial() and addEffect()
note that packs themselves do NOT grant the items, and selling them to rebuy them doesnt work


set(slot, name, sprite, tokenValue, heroMask)
addSpecial(effectID, target)
addEffect(effectID, target, value)